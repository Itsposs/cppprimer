

#include <string>
#include <vector>
#include <iostream>


std::vector<std::string> func(std::string &s)
{
	if ( s = std::string("hello"))
		return {"ok,", " hello"};
	else
		return {"error,", "hello"};
}


int main(int argc, char *argv[])
{
	return 0;
}
