


void Quick(std::vector<int> &vec)
{
	using std::vector<int>::size_type size_type;
	//typedef std::vector<int>::size_type sizet_ype;
	
	sizet_ype left = 0;
	sizet_ype right = vec.size() - 1;
	while(left != right)
	{

	}
}
