

#include <cassert>




char * strcpy(char *to, const char *from)
{
	assert(to != NULL && from != NULL);
	
	//char *p = to;  // error
	//while((*p++ = *from++) != '\0')
	//	;
	//return to;
	

}



#include <iostream>

int main(int argc, char *argv[])
{
	return 0;
}
