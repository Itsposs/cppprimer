#include <iostream>
#include <vector>

int main(int argc, char *argv[])
{
	constexpr int mf = 20;
	constexpr int limit = mf + 1;
	std::vector<int> vec = {1, 3, 5, 7};
	//constexpr int sz = vec.size();
	return 0;
}
